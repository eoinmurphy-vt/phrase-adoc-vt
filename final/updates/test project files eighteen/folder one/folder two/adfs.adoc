= Testprojekt achtzehn SAML (ADFS)
:revdate: 2025-05-08
:page-revdate: {revdate}
:page-opendocs-origin: /08.integration/06.adfs/06.adfs.md
:page-opendocs-slug:  /integration/adfs

== Test_ZweiEinrichtung von ADFS und {product-name} Integration

Dieser Abschnitt beschreibt zuerst die Einrichtungsschritte in ADFS und dann in der {product-name} Konsole.

=== Test_TwoADFS Setup

. Klicken Sie mit der rechten Maustaste auf {Q}`Relying Party Trusts`{/Q} in der AD FS-Verwaltung und wählen Sie {Q}`Add Relying Party Trust...`{/Q}.
+
image:adfs1.png[adfsSetup]

. Wählen Sie die Schaltfläche {Q}`Start`{/Q} im Willkommensschritt aus.
+
image:adfs2.png[adfsSetup]

. Wählen Sie {Q}`Enter data about the relying party manually`{/Q} aus und wählen Sie {Q}`Next`{/Q}.
+
image:adfs3.png[adfsSetup]

. Geben Sie einen eindeutigen Namen für das Feld Anzeigename ein und wählen Sie {Q}`Next`{/Q} aus.
+
image:adfs4.png[adfsSetup]

. Wählen Sie {Q}`Next`{/Q} aus, um die Token-Verschlüsselung zu überspringen.
+
image:adfs5.png[adfsSetup]

. Überprüfen Sie {Q}`Enable support for the SAML 2.0 WebSSO protocol`{/Q} und geben Sie die SAML-Redirect-URI aus der {product-name} Einstellungen > SAML-Einstellungsseite in das Feld {Q}`Relying party SAML 2.0 SSO service URL`{/Q} ein.  Wählen Sie {Q}`Next`{/Q}, um fortzufahren.
+
image:adfs6.png[adfsSetup]

. Geben Sie dieselbe SAML-Redirect-URI in das Feld {Q}`Relying party trust identifier`{/Q} ein und klicken Sie auf {Q}`Add`{/Q}; wählen Sie dann {Q}`Next`{/Q}, um fortzufahren.
+
image:adfs7.png[adfsSetup]

. Passen Sie die Zugriffskontrolle an; wählen Sie dann {Q}`Next`{/Q}, um fortzufahren.
+
image:adfs8.png[adfsSetup]

. Wählen Sie {Q}`Next`{/Q}, um fortzufahren.
+
image:adfs9.png[adfsSetup]

. Wählen Sie {Q}`Close`{/Q}, um abzuschließen.
. Wählen Sie die Bearbeitung der Anspruchsausstellungspolitik ... aus.
+
image:adfs10-11.png[adfsSetup]

. Wählen Sie {Q}`Add Rule...`{/Q} aus und wählen Sie {Q}`Send LDAP Attributes as Claims`{/Q}; wählen Sie dann {Q}`Next`{/Q}.  Benennen Sie die Regel und wählen Sie Active Directory als Attributspeicher aus. Nur der ausgehende Anspruch des Benutzernamens ist für die Authentifizierung erforderlich, wenn die Standardrolle festgelegt ist; andernfalls sind Gruppen für die Rollenzuordnung erforderlich.  E-Mail ist optional.
+
--
* SAM-Konto-Name -> Benutzername
* E-Mail-Adresse -> E-Mail
* Token-Gruppen -- Unqualifizierte Namen -> Gruppen

image:adfs11-12.png[adfsSetup]
--
. Wählen Sie {Q}`Add Rule...`{/Q} aus und wählen Sie {Q}`Transform an Incoming Claim`{/Q}; wählen Sie dann {Q}`Next`{/Q}.  Benennen Sie die Regel und setzen Sie das Feld wie im Screenshot unten fest.  Das ausgehende Name-ID-Format muss Transient Identifier sein.
+
image:adfs12-13.png[adfsSetup]

=== Test_Two{product-name} Setup

. Identifizieren Sie die Anbieter-SSO-URL
* Zeigen Sie Endpunkte aus AD FS-Verwaltung > Dienst an und verwenden Sie die {Q}`SAML 2.0/WS-Federation`{/Q} Endpunkt-URL.
* Beispiel: `+https://<adfs-fqdn>/adfs/ls+`

. Identitätsanbieter-Aussteller
* Klicken Sie mit der rechten Maustaste auf AD FS in der AD FS-Verwaltungskonsole und wählen Sie {Q}`Edit Federation Service Properties...`{/Q}; verwenden Sie die {Q}`Federation Service identifier`{/Q}.
* Beispiel: `+http://<adfs-fqdn>/adfs/services/trust+`

. X.509-Zertifikat
* Wählen Sie in der AD FS-Verwaltung Dienst > Zertifikat, klicken Sie mit der rechten Maustaste auf das Token-Signierungszertifikat und wählen Sie {Q}`View Certificate...`{/Q}
* Wählen Sie die Registerkarte Details und klicken Sie auf {Q}`Copy to File`{/Q}
* Speichern Sie es als eine Base-64-codierte x.509 (.CER) Datei
* Kopieren Sie den Inhalt der Datei und fügen Sie ihn in das X.509-Zertifikat-Feld ein

. Gruppenanspruch
* Geben Sie den ausgehenden Anspruchsnamen für die Gruppen ein
* Beispiel: Gruppen

. Standardrolle
* Es wird empfohlen, {Q}`None`{/Q} zu sein, es sei denn, Sie möchten, dass jeder authentifizierte Benutzer eine Standardrolle hat.

. Rollenkarte
* Legen Sie die Gruppennamen der Benutzer für die entsprechende Rolle fest.  (Siehe Screenshot-Beispiel unten.)
+
image:nv_adfs1.png[NVadfsSetup]

=== Test_TwoMapping Gruppen zu Rollen und Namespaces

Bitte sehen Sie sich den Abschnitt xref:users.adoc#_mapping_groups_to_roles_and_namespaces[Benutzer und Rollen] an, um zu erfahren, wie Gruppen auf vordefinierte und benutzerdefinierte Rollen sowie Namespaces in {product-name} abgebildet werden.

== Test_TwoTroubleshooting

. ADFS SamlResponseSignature muss entweder MessageOnly oder MessageAndAssertion sein.  Verwenden Sie den Befehl Get-AdfsRelyingPartyTrust, um ihn zu überprüfen oder zu aktualisieren.
+
image:nv_adfs2.png[adfsTroubleshooting]

. Zeit-Synchronisation zwischen Kubernetes-Knoten und ADFS-Server

Für eine erfolgreiche Authentifizierung muss die Zeit zwischen den Kubernetes-Knoten und dem ADFS-Server gleich sein, um Probleme mit der Zeit-Synchronisation oder Zeitabweichungen zu vermeiden.

Es wird empfohlen, einen https://en.wikipedia.org/wiki/Network_Time_Protocol[NTP-Server] zu verwenden, mit gleichen Zeiteinstellungen auf allen Servern.

Bitte überprüfen und bestätigen Sie, dass sowohl ADFS als auch {product-name} Hosts synchronisiert sind und die potenziellen Verzögerungen 10 Sekunden nicht überschreiten. Sie können Linux- und Windows-Befehle verwenden, um Daten, Zeiten und die Aktivität des NTP-Servers zu überprüfen.

[TIP]
====
Sie können die Authentifizierungszeiten neu laden, indem Sie die Konfiguration im {product-name} UI wie folgt deaktivieren und erneut aktivieren:

* Melden Sie sich bei {product-name} mit dem Admin-Benutzer an
* Gehen Sie zu Einstellungen
* Klicken Sie auf die Schaltfläche, um die SAML-Einstellung zu deaktivieren und zu aktivieren
** *Stellen Sie sicher, dass Sie die Konfigurationseinstellungen beibehalten!*

Sobald die Einstellung wieder aktiviert ist, können Sie versuchen, sich mit einem ADFS-Benutzer anzumelden. Wenn es funktioniert, bestätigt dies, dass das Problem auf einen Zeit-Synchronisierungsfehler zwischen den Kubernetes-Knoten und dem ADFS-Server zurückzuführen war.
====

. SAML-Zeichen müssen in der {product-name} UI groß- und kleinschreibung beachten.
+
Attributnamen sind groß- und kleinschreibung beachten. Stellen Sie sicher, dass jeder hier konfigurierte SAML-Attributname genau mit der Anwendungs-Konfiguration übereinstimmt. SAML muss auf die richtige URL zur Authentifizierung verweisen.
+
Alle Felder in `+{product-name} UI -> Settings -> SAML Settings+` sind groß- und kleinschreibung beachten.
+
Die Protokolle des {product-name} Controllers enthalten die relevanten Informationen zur Authentifizierung mit dem ADFS-Server und Fehler, die helfen, die Ursache zu identifizieren. Wir empfehlen, die fehlgeschlagene Anmeldesituation zu reproduzieren und die Protokolle zu überprüfen.

. Stellen Sie sicher, dass Sie die richtigen Gruppen, Zertifikate und Protokolle eingeben.
+
Die SAML-Einstellungen müssen mit der folgenden Konfiguration übereinstimmen:
+
|===
| Einstellung | Wert

| Identifizieren Sie die Anbieter-SSO-URL
| Erfordert HTTPS-Protokoll

| Identitätsanbieter-Aussteller
| Erfordert HTTP-Protokoll

| ADFS SamlResponseSignature
| Muss entweder MessageOnly oder MessageAndAssertion sein.
|===

[CAUTION]
====
Diese Einstellungen müssen auf Ihrem ADFS-Server und in der {product-name} UI validiert werden.
====

Das ausgewählte Zertifikat muss gültig und korrekt generiert sein, einschließlich seiner `CA Root` und `Intermediate Certificates`. Sie können sie mit Ihrer vertrauenswürdigen Zertifizierungsstelle, Windows oder einem Automatisierungstool wie https://letsencrypt.org/[LetsEncrypt] generieren.

Wenn einer dieser Parameter falsch ist, erhalten Sie einen `Authentication Failed` Fehler, wenn Sie versuchen, sich bei {product-name} mit einem ADFS-Benutzer über SAML-Authentifizierung anzumelden.
